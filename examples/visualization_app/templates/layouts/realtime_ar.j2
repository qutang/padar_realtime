<html>
{% with title=title %}
    {% include 'components/head.j2' %}
{% endwith %}
<body>
{% with title=title %}
    {% include 'components/navBar.j2' %}
{% endwith %}
<div class='container-fluid'>
    <h2>Signals</h2>
    <div class='card-group'>
    {% for i in range(config.max_sensors) %}
        {% with values=values, chart_id="sensor_time_series_chart_" ~ i, title="Sensor signal:" ~ config.url ~ ':' ~ (config.init_port + i), url=config.url, port=config.init_port + i, refresh_rate = config.refresh_rate %}
            {% include 'components/sensor_time_series_chart.j2' %}
        {% endwith %}
    {% endfor %}
    </div>
    <h2>Features and predictions</h2>
    <div class='card-group'>
    {% with values=values, chart_id="feature_time_series_chart", title="Features time series", url=config.url, port=config.init_ar_port, refresh_rate = config.refresh_rate %}
        {% include 'components/feature_time_series_chart.j2' %}
    {% endwith %}
    </div>
    </div>
    
</div>
</body>
</html>